import FacebookIcon from './facebook-icon.svg';
import GoogleIcon from './google-icon.svg';

const facebookIcon = String(FacebookIcon);
const googleIcon = String(GoogleIcon);

export const icons = {
	facebookIcon,
	googleIcon,
};
